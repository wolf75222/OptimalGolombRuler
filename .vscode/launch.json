{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OpenMP",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/golomb_openmp.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "Build OpenMP (MSVC)"
        }
    ]
}
